[package]
name = "verryte-audio"
version = "0.1.0"
edition = "2021"

[dependencies]
rodio = "0.17"
verryte-core = { path = "../verryte-core" }
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
